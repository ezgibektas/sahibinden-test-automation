# Spring Configuration
spring.main.allow-bean-definition-overriding=true

# Test Configuration
test.webdriver.bean=webDriver
test.timeout=30
test.retry.count=3
test.screenshot.enabled=true

# Browser Configuration
browser.type=chrome
browser.headless=false
browser.implicit.wait=20
browser.page.load.timeout=30
browser.script.timeout=30
browser.window.maximize=true
selenium.timeout=60

# Desktop Configuration
test.responsive.mode=false
test.browser.width=1920
test.browser.height=1080

# Screenshot Configuration
screenshot.path=target/screenshots
screenshot.on.failure=true

# Logging Configuration
logging.level.com.sahibinden=DEBUG

# Test Configuration
test.base.url=https://www.sahibinden.com
test.screenshot.path=target/screenshots/
test.timeout.seconds=30

# Browser Configuration
browser=chrome
browser.headless=false
browser.implicit.wait=20
browser.page.load.timeout=30
browser.script.timeout=30

# Logging Configuration
logging.level.org.openqa.selenium=INFO
logging.level.org.springframework=INFO

# Spring Configuration
spring.main.allow-bean-definition-overriding=true

# Selenoid configuration (if needed)
#selenoid.url=http://localhost:4444/wd/hub

# Logging
logging.level.root=INFO

# Test Configuration
#selenium.grid.url=http://localhost:4444/wd/hub